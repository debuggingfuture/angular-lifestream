require(['angular', 'masonry', '../example-config', 'angular-route', 'angular-animate', 'angular-sanitize', 'angular-lifestream'], function(angular, Masonry,exampleConfig) {
    console.log('bootstrap site');
    console.log(exampleConfig);

    // angular.bootstrap(document, ["site"]);


});